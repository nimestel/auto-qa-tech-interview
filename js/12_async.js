/**
 *   TODO доработать
 *
 *   если в функции будут await, функция должна быть async
 *   если промис await что-то возвращает, мы должны присвоить этот результат как const response =
 *  await fetch(...
 *
 *   await позволяет не переходить к новой строчке, пока промис предыдущей не выполнится
 *   функция async всегда возвращает промис
 *
*/